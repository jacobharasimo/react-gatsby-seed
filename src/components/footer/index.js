import React from 'react';
import { Box, Flex } from 'rebass';

export const Footer = () => (
  <Flex color="white" bg="grey" py={2}>
    <Box variant="container">
      <Flex>
        <Box>
          This is the footer
        </Box>
      </Flex>
    </Box>
  </Flex>
);
